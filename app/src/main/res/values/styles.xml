<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- TODO: change when app support lib supports accent colors etc. properly -->
        <item name="android:listViewStyle">@style/overScrollLessListView</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:actionBarStyle">@style/pink_action_bar</item>
        <item name="actionBarStyle">@style/pink_action_bar</item>
        <item name="android:listViewStyle">@style/pink_list_view</item>
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">@color/highlight</item>
    </style>

    <style name="pink_action_bar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@color/highlight</item>
        <item name="background">@color/highlight</item>
    </style>

    <style name="pink_list_view" parent="@android:style/Widget.ListView">
        <item name="android:listSelector">@drawable/list_selector</item>
    </style>

    <style name="overScrollLessListView" parent="@style/pink_list_view">
        <item name="android:overScrollMode">never</item>
    </style>

</resources>
